{
    "name": "perf-benchmarks-components",
    "version": "2.1.0",
    "private": true,
    "scripts": {
        "build": "rm -fr dist && rollup -c"
    },
    "devDependencies": {
        "@lwc/rollup-plugin": "2.1.0"
    }
}
