{
    "name": "@lwc/perf-benchmarks-components",
    "version": "5.2.1-alpha.0",
    "private": true,
    "scripts": {
        "build": "rm -fr dist && rollup -c ./rollup.config.mjs"
    },
    "devDependencies": {
        "@lwc/rollup-plugin": "5.2.1-alpha.0"
    },
    "nx": {
        "targets": {
            "build": {
                "outputs": [
                    "{projectRoot}/dist"
                ]
            }
        }
    }
}
